[package]
name = "rookery"
description = "Fleet control for WebLinked instances over OSC"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[[bin]]
name = "rookery"
path = "src/main.rs"

[dependencies]
diag = { path = "../diag" }
rookery-core = { path = "../core" }
rookery-discovery = { path = "../discovery" }
rookery-fleet = { path = "../fleet" }
rookery-instance-live = { path = "../instance-live" }
rookery-osc = { path = "../osc" }
rookery-web = { path = "../web" }
anyhow.workspace = true
axum.workspace = true
serde.workspace = true
toml.workspace = true
tokio.workspace = true
tracing.workspace = true
