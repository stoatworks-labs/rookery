[package]
name = "rookery-web"
description = "REST API, live-push websocket and embedded control page for rookery"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
rookery-core = { path = "../core" }
rookery-discovery = { path = "../discovery" }
rookery-fleet = { path = "../fleet" }
anyhow.workspace = true
axum.workspace = true
serde.workspace = true
serde_json.workspace = true
tokio.workspace = true
tracing.workspace = true
jpeg-encoder.workspace = true

[dev-dependencies]
jpeg-decoder.workspace = true
# For tests/router.rs: building an AppState needs a real client provider.
rookery-instance-live = { path = "../instance-live" }
